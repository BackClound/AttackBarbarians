## 阶段五：数据持久化和优化

### 模板 8：实现数据持久化系统
目标
实现无限防守模式+肉鸽类游戏的数据保存系统，支持玩家进度、玩家局外属性永久升级及玩家局内基础属性及技能buff数据及玩家金币，钻石，签到数据等的持久化。

上下文
使用 JSON 序列化结合 PlayerPrefs 或本地文件存储

存档数据包括：
全局数据：玩家金币，钻石，签到数据，四维基本属性数值，技能等级数据等
游戏局内临时数据：当前波次，当前等级，当前已选buff列表，player当前血量等

支持数据导入/导出功能，便于测试调优

输出要求
生成 SaveData 数据结构（Serializable class）：

permanentUpgrades (Dictionary<string, int> 或 List<PermanentUpgradeData>)

生成 SaveManager.cs：

单例模式管理存档

SaveGame() 将数据序列化为 JSON，存储到 Application.persistentDataPath

LoadGame() 加载 JSON，如果文件不存在则创建默认数据

提供增量和立即保存的接口

生成 PermanentUpgradeSO.cs：

buff效果在游戏启动时应用到 UpgradeManager 的全局加成中

数据保存触发时机
每波完成时自动保存（避免游戏中途崩溃丢失进度）

玩家手动保存/退出时保存